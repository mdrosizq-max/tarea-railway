const express = require('express'); const mysql = require('mysql2'); const app = express(); const port = process.env.PORT || 3000; const connection = mysql.createPool(process.env.MYSQL_URL); app.get('/', (req, res) => { connection.query('SELECT * FROM usuarios', (err, results) => { let filas = ''; if (results) { results.forEach(u => { filas += `<tr><td>${u.id}</td><td>${u.nombre}</td><td>${u.email}</td></tr>`; }); } res.send(`<html><body style='font-family:sans-serif; background:#0b0e14; color:white; padding:2rem;'><div style='max-width:800px; margin:auto; background:#151921; padding:2rem; border-radius:15px; border:1px solid #30363d;'><h1 style='color:#007bff;'>📋 CRUD Usuarios</h1><p>Estado: <span style='color:#2ea043;'>CONECTADO</span></p><table style='width:100%; border-collapse:collapse;'><thead><tr style='background:#0d1117;'><th>ID</th><th>Nombre</th><th>Email</th></tr></thead><tbody>${filas || '<tr><td colspan="3">No hay datos todavía</td></tr>'}</tbody></table></div></body></html>`); }); }); app.listen(port, () => console.log('Servidor CRUD listo'));
