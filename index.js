const express = require('express'); const { Pool } = require('pg'); const app = express(); const port = process.env.PORT || 3000; const pool = new Pool({ connectionString: process.env.DATABASE_URL }); app.get('/', async (req, res) => { try { const result = await pool.query('SELECT NOW() as hora, version() as version'); const { hora, version } = result.rows[0]; res.send(`<html><body style='font-family:sans-serif; background:#121212; color:white; display:flex; justify-content:center; align-items:center; height:100vh;'><div style='background:#1e1e1e; padding:2rem; border-radius:12px; border:1px solid #333;'><h1 style='color:#00cf7f;'>🚀 Railway Dashboard</h1><p>Estado: <span style='color:#00cf7f; font-weight:bold;'>CONECTADO</span></p><hr style='border:0; border-top:1px solid #333;'><p><b>Hora DB:</b> ${hora}</p><p><b>Motor:</b> PostgreSQL</p><p style='font-size:0.7rem; color:#666;'>${version}</p></div></body></html>`); } catch (err) { res.status(500).send('<h1>Error DB</h1><p>' + err.message + '</p>'); } }); app.listen(port, () => console.log('Servidor listo'));
